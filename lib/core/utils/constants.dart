import 'package:flutter/material.dart';

class AppConstants {
  static const Color voltisDark = Color.fromRGBO(30, 30, 30, 1);
  static const Color voltisLight = Color.fromRGBO(255, 255, 255, 1);
  static const Color voltisAccent = Color.fromRGBO(171, 40, 178, 1);
  
  static const Color labelTextDark = Color.fromRGBO(200, 200, 200, 1);
  static const Color labelTextLight = Color.fromRGBO(100, 100, 100, 1);
  
  static const Color borderDark = Color.fromRGBO(45, 45, 45, 1);
  static const Color borderLight = Color.fromRGBO(230, 230, 230, 1);

  static const Color dividerColor = Color.fromRGBO(151, 151, 151, 0.5);
  static const Color filterColor = Color.fromRGBO(171, 40, 178, 0.5);
  static const Color txtColor = Color.fromRGBO(151, 151, 151, 1);

  // Animation Durations
  static const Duration shortAnimation = Duration(milliseconds: 200);
  static const Duration mediumAnimation = Duration(milliseconds: 300);
  
  // Spacing
  static const double defaultSpacing = 16.0;
  static const double smallSpacing = 8.0;
  
  // Border Radius
  static const double borderRadius = 8.0;
  static const double cardRadius = 12.0;
}